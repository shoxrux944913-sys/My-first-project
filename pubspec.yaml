name: flutter_application_2_dars
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  # ТВОИ ПРЕДЫДУЩИЕ ПАКЕТЫ
  cached_network_image: ^3.3.1
  google_fonts: ^6.0.0  
  cupertino_icons: ^1.0.8
  go_router: ^14.0.0
  http: ^1.2.0
  firebase_core: ^3.10.0
  cloud_firestore: ^5.6.0
  provider: ^6.1.1
  flutter_bloc: ^8.1.6
  flutter_riverpod: ^2.5.1
  dio: ^5.4.1
  shared_preferences: ^2.2.2

  # НОВЫЕ ПАКЕТЫ ДЛЯ МОДЕЛЕЙ (Freezed)
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  # ГЕНЕРАТОРЫ (Которые пишут код за тебя)
  build_runner: ^2.4.13
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true
  assets:
    - lib/images/
